#ifndef DEFS_H
#define DEFS_H

#define ARRAY_LEN(array) (sizeof(array) / sizeof(array[0]))

typedef int           i32;
typedef unsigned int  u32;
typedef long          i64;
typedef unsigned long u64;

typedef float f32;

#endif // DEFS_H
